# Runtime
openai>=1.0
python-dotenv>=1.0
rich>=13.0
pyyaml>=6.0          # rulepack.py
qwen-agent
mcp>=1.0             # mcp_server.py — the deterministic, keyless MCP server (stdio)
# qwen-agent imports these at runtime but does not declare them:
numpy
soundfile
python-dateutil

# Windows TLS: if API calls fail with CERTIFICATE_VERIFY_FAILED, this bridges the
# Windows certificate store into Python (see docs/09-manual-requirements.md).
# Harmless / skipped on non-Windows.
pip-system-certs; platform_system == "Windows"

# Optional — only needed for listen.py's record_from_mic() (live microphone
# capture). Not installed by default: PortAudio can be finicky on some
# machines, and everything else in listen.py works on any audio file you
# already have. `pip install sounddevice` if you want to record live.
# sounddevice

# Dev
pytest>=8.0
