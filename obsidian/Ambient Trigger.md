---
tags: [module]
source: "Trace/ambient.py, Trace/watch_rules.yaml"
---

# Ambient Trigger

One brain, two worlds. An allowlist matcher maps window/document titles to
project contexts. Called by BOTH trigger paths, so the browser demo and the
real desktop watcher are provably the same logic.

## Related

- Called by [[Desktop Watcher]] (real window titles).
- Called by [[Workspace Demo]] (simulated drawings in a browser).
- Both paths push events into [[Ambient Bubble]].
